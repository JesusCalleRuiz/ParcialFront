import { FreshContext, Handlers } from "$fresh/server.ts";
import Form from "../components/Form.tsx"

const Page = () => {
    return (
        <div>
            <Form/>
        </div>
    )
}

export default Page;